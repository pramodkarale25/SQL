create table EMPLOYEE
(
EMPID INT PRIMARY KEY,
EMPNAME VARCHAR(100) NOT NULL,
DEPTID INT FOREIGN KEY REFERENCES DEPARTMENT(DEPTID),
EMAIL VARCAHR(100) UNIQUE,
MOBILE INT UNIQUE,
SALARY INT DEFAULT(0),

)